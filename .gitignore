# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# Distribution / build
build/
dist/

# OS
.DS_Store
Thumbs.db

# Claude Code local settings (machine-specific, not shared context)
.claude/settings.local.json
CLAUDE.md

# Local-only architecture notes (not part of the public deliverable)
NOTES.md

# Simulation outputs (generated by running the sim, not source) — keep the directory and
# its README checked in so the expected location is discoverable; ignore everything a run
# actually generates inside it.
output/*
!output/README.md
*.log
